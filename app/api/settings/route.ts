import{NextResponse}from'next/server';import{db,sb}from'../../../lib/supabase';import{ok}from'../../../lib/auth';export async function GET(){if(!db)return NextResponse.json({qris_url:'',banner_url:'',audio_url:''});const r=await sb('settings?select=key,value');return NextResponse.json(Object.fromEntries((await r.json()).map((x:any)=>[x.key,x.value])))}export async function POST(q:Request){if(!ok())return NextResponse.json({error:'unauthorized'},{status:401});const x=await q.json();if(db)await sb('settings',{method:'POST',headers:{Prefer:'resolution=merge-duplicates'},body:JSON.stringify(Object.entries(x).map(([key,value])=>({key,value})))});return NextResponse.json(x)}