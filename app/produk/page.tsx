'use client';import Link from 'next/link';import {useEffect,useState} from 'react';export default function P(){const[p,setP]=useState<any[]>([]),[q,setQ]=useState('');useEffect(()=>{fetch('/api/products').then(r=>r.json()).then(setP)},[]);const a=p.filter(x=>(x.name+x.cat).toLowerCase().includes(q.toLowerCase()));return <div className="wrap"><div className="card"><h1>Produk</h1><input placeholder="Cari produk..." value={q} onChange={e=>setQ(e.target.value)} style={{width:'100%',padding:12}}/></div><div className="grid" style={{gridTemplateColumns:'repeat(auto-fit,minmax(220px,1fr))',marginTop:14}}>{a.map(x=><Link className="card" href={'/produk/'+x.slug} key={x.slug}><span className="pill">{x.cat}</span><h3>{x.name}</h3><b>Rp{x.price.toLocaleString('id-ID')}</b><p className="muted">{x.desc}</p></Link>)}</div></div>}