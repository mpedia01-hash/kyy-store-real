'use client';import{useEffect,useState}from'react';export default function D({params}:{params:{slug:string}}){const[p,setP]=useState<any>();useEffect(()=>{fetch('/api/products').then(r=>r.json()).then(a=>setP(a.find((x:any)=>x.slug===params.slug)))},[params.slug]);if(!p)return <div className="wrap"><div className="card">Memuat...</div></div>;return <div className="wrap"><div className="card"><span className="pill">{p.cat}</span><h1>{p.name}</h1><p>{p.desc}</p><h2>Rp{Number(p.price).toLocaleString('id-ID')}</h2><a className="btn" href={'/checkout?product='+p.slug}>Beli Sekarang</a></div></div>}