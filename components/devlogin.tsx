'use client';import{useState}from'react';export function DevLogin({ok}:{ok:(x:boolean)=>void}){const[k,setK]=useState('');return <div className="card"><h1>Developer Login</h1><input type="password" placeholder="Developer Key" value={k} onChange={e=>setK(e.target.value)} style={{width:'100%',padding:12}}/><button className="btn" onClick={async()=>{const r=await fetch('/api/auth',{method:'POST',body:JSON.stringify({key:k})});ok(r.ok)}}>Masuk</button></div>}