'use client';import{useState}from'react';export function QuranUI(){const[p,setP]=useState('/sholat'),[d,setD]=useState<any>();async function go(){setD(await fetch('/api/myquran'+p).then(r=>r.json()))}return <><div className="card"><h1>📖 Quran & Muslim</h1><p className="muted">MyQuran v3 mendokumentasikan Sholat, Kalender, Qibla, Tools; alQuran saat ini masih segera.</p><button className="btn" onClick={()=>setP('/sholat')}>🕌</button> <button className="btn" onClick={()=>setP('/kalender')}>📅</button> <button className="btn" onClick={()=>setP('/qibla')}>🧭</button><input value={p} onChange={e=>setP(e.target.value)} style={{width:'100%',padding:12,marginTop:12}}/><button className="btn" onClick={go}>Ambil</button></div>{d&&<pre className="card">{JSON.stringify(d,null,2)}</pre>}</>}