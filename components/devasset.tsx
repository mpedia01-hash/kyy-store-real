'use client';import{useState}from'react';export function DevAsset(){const[f,setF]=useState<File>(),[t,setT]=useState('qris_url');async function up(){if(!f)return;const r=await fetch('/api/assets',{method:'POST',headers:{'x-file-name':f.name,'Content-Type':f.type},body:await f.arrayBuffer()}).then(r=>r.json());if(r.url)await fetch('/api/settings',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({[t]:r.url})});alert('Tersimpan')}return <><select value={t} onChange={e=>setT(e.target.value)}><option value="qris_url">QRIS</option><option value="banner_url">Banner</option><option value="audio_url">Backsound</option></select><input type="file" onChange={e=>setF(e.target.files?.[0])}/><button className="btn" onClick={up}>Upload</button></>}