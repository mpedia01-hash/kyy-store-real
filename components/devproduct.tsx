'use client';import{useEffect,useState}from'react';export function DevProduct(){const[p,setP]=useState<any[]>([]);const load=async()=>setP(await fetch('/api/products').then(r=>r.json()));useEffect(()=>{load()},[]);async function add(){await fetch('/api/products',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({slug:prompt('slug?'),name:prompt('nama?'),cat:prompt('kategori?'),price:Number(prompt('harga?')),desc:prompt('deskripsi?')})});load()}return <><button className="btn" onClick={add}>+ Produk</button>{p.map(x=><p key={x.slug}>{x.name} — Rp{Number(x.price).toLocaleString('id-ID')}</p>)}</>}