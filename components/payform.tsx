'use client';import{useState}from'react';export function PayForm({p}:{p:any}){const[n,setN]=useState('');async function buy(){const x=await fetch('/api/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({slug:p.slug,name:n,product:p.name,total:p.price})}).then(r=>r.json());location.href='https://wa.me/'+(process.env.NEXT_PUBLIC_WA_NUMBER||'')+'?text='+encodeURIComponent('Halo Admin KYY STORE REAL!\nID Order: '+x.id+'\nProduk: '+p.name+'\nTotal: Rp'+p.price.toLocaleString('id-ID')+'\nBukti pembayaran akan saya kirim di chat.')}return <><input placeholder="Nama / data order" value={n} onChange={e=>setN(e.target.value)} style={{width:'100%',padding:12}}/><button className="btn" onClick={buy}>Sudah Bayar → WhatsApp</button></>}